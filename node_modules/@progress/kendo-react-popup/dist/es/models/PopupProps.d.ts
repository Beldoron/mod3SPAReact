import { PopupSettings } from './PopupSettings';
/**
 * Represents the props of the [Kendo UI Popup component for React]({% slug overview_popup %}).
 */
export interface PopupProps extends PopupSettings {
    /**
     * Controls the Popup visibility ([see example]({% slug hidden_popup %})).
     * Defaults to `false`.
     */
    show?: boolean;
}
